spring:
  application:
    name: crud
  datasource:
    url: jdbc:h2:mem:meubanco  #Alterar nome do banco
    driver-class-name: org.h2.Driver
    username: Andrew
    password: 123456
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: update
